description: A representation of a dog
allOf:
  - $ref: ./WebhookBody.yaml
  - type: object
    properties:
      SKUName:
        type: string
        example: "YourNextU Test SKU"
      packSize:
        type: integer
        format: int32
        description: The size of the pack the dog is from
        default: 1
        minimum: 1
    required:
      - SKUName
